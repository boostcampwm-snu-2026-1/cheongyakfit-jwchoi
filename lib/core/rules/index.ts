// 룰 타입 (소득/가점/예치금). 값은 DB rule 테이블에서 로드해 엔진에 인자로 주입 (코드 하드코딩 X — decisions.md D16).
// 룰의 구조·근거 SSoT는 docs/domain/. 시드/마이그레이션에 역참조 주석. 예) // SSoT: docs/domain/eligibility.md §예치금표 (2026 기준)
export {};
